---
id: "T-0113"
source: "docs/portal-specs/01-feature-epics/EPIC-006-remediation-engine/SPEC.md"
section: "UI — apply & history"
severity: "high"
status: "open"
original_status: "declared"
parent: "EPIC-006"
scope:
  - portal/web/src/components/remediation/ApplyActionDialog.tsx
  - portal/web/src/components/remediation/ApplyActionDialog.test.tsx
  - portal/web/src/app/remediation/history/page.tsx
  - portal/web/src/components/remediation/RemediationHistoryTable.tsx
tests:
  - portal/web/src/components/remediation/ApplyActionDialog.test.tsx
related: []
depends_on:
  - T-0108
needs_scope_review: false
scope_note: ""
---

# T-0113 — Apply confirmation dialog and remediation history view

## Report

EPIC-006 SPEC.md §3.3: an `ActionDialog` lists the selected checkIds and target tenants, with a
second confirmation for batch applies showing the count. Fields: reason (required), `Dry run`
switch (default on). On confirm, results render inline (`.status-badge` per action) with a
**View audit** link. SPEC.md §3.4: nav *Tenant Administration → Remediation → History*, an
append-only table with timestamp, actor, tenant, check ID, command, before → after, result, and
correlation ID; export to SIEM is EPIC-041.

Build `ApplyActionDialog` (calling `POST /plans/{planId}/apply` with a generated
`Idempotency-Key` and the dialog's `dryRun` value) and the `history/page.tsx` +
`RemediationHistoryTable` reading `GET /v1/remediation/history`. Reuse the ActionDialog/status
badge components.

## Acceptance

- [ ] Confirmation lists the selected checkIds/tenants; a batch apply shows a second count confirmation.
- [ ] `Dry run` defaults on; `reason` is required before confirm.
- [ ] Results render inline per action and link to the audit trail.
- [ ] History renders the append-only columns from the history endpoint.
- [ ] A test covers the change and fails without it.
- [ ] No file outside `scope` is modified.
