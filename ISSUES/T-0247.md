---
id: "T-0247"
source: "docs/portal-specs/01-feature-epics/EPIC-013-roles-pim-jit/SPEC.md"
section: "JIT admin templates"
severity: "medium"
status: "open"
original_status: "declared"
parent: "EPIC-013"
scope:
  - portal/db/migrations/
  - portal/db/src/jit-templates-repository.ts
  - portal/db/src/repository.ts
  - portal/bff/src/routes/jit-templates.ts
  - portal/bff/src/routes/jit-templates.test.ts
tests:
  - portal/bff/src/routes/jit-templates.test.ts
related: []
depends_on:
  - T-0246
needs_scope_review: false
scope_note: ""
---

# T-0247 — JIT admin/role templates CRUD

## Report

EPIC-013 SPEC.md §3.4 and §5 (US-6): templates define allowed roles, duration, justification,
and approval; row actions include `Edit template`. §5 defines `JitAdminTemplate` (`id`, `name`,
`allowedRoles[]`, `duration`, `justificationRequired`, `approvalRequired`).

Add the `JitAdminTemplate` entity via the next forward-only migration and a
`jit-templates-repository.ts`, then expose `GET`/`POST`/`PATCH`/`DELETE` `/v1/jit-templates` in
`jit-templates.ts`. A grant (T-0246) resolves its allowed roles, duration, and approval from
these templates; template changes never touch a tenant. Extend `portal.v1.yaml`.

## Acceptance

- [ ] Templates CRUD persists allowed roles, duration, and the justification/approval flags.
- [ ] A grant referencing a template outside its allowed roles is rejected.
- [ ] Deleting a template with active grants is refused or reports the affected grants.
- [ ] A test covers the change and fails without it.
- [ ] No file outside `scope` is modified.
