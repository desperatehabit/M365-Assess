---
id: "T-0183"
source: "docs/portal-specs/01-feature-epics/EPIC-010-baselines-rollouts/SPEC.md"
section: "Baselines UI"
severity: "high"
status: "open"
original_status: "declared"
parent: "EPIC-010"
scope:
  - portal/web/src/app/baselines/[id]/edit/page.tsx
  - portal/web/src/components/baselines/BaselineTimeline.tsx
  - portal/web/src/components/baselines/StageEditor.tsx
  - portal/web/src/components/baselines/BaselineBuilder.test.tsx
  - portal/web/src/lib/baselinesApi.ts
tests:
  - portal/web/src/components/baselines/BaselineBuilder.test.tsx
related: []
depends_on:
  - T-0182
needs_scope_review: false
scope_note: ""
---

# T-0183 — Baseline builder UI

## Report

EPIC-010 SPEC.md §3.3 defines Add/Edit Baseline with sidebar cards **Baseline Details**,
**Alerting**, **Setup Progress**, and **Baseline Summary**. The timeline steps are *Set a baseline
name → Assign tenants or groups → Add standards to at least one stage*, with **save disabled until
all three are done**. Stages are added/removed in order; each stage holds standards with
`logic: 'and'` and conditions, each condition individually removable (US-1, US-2). Alerting config
ties into EPIC-029 (prose).

Deliver the builder route, `BaselineTimeline`, `StageEditor`, and extend `baselinesApi.ts` wired to
T-0182. Stage/condition editing must not permit a non-`and` logic value.

## Acceptance

- [ ] The three timeline steps render and gate the save button as specified.
- [ ] Stages are ordered with add/remove; conditions are individually removable and only `and` logic is offered.
- [ ] A test covers the builder interactions and fails without it; only theme tokens are used.
- [ ] No file outside `scope` is modified.
