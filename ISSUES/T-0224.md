---
id: "T-0224"
source: "docs/portal-specs/01-feature-epics/EPIC-012-mfa-auth-methods/SPEC.md"
section: "Push & default method"
severity: "medium"
status: "open"
original_status: "declared"
parent: "EPIC-012"
scope:
  - portal/bff/src/routes/mfa.ts
  - portal/bff/src/routes/mfa-actions.test.ts
  - portal/workers/M365Portal.Workers/Invoke-MfaAction.ps1
  - portal/workers/invoke-mfa-action.ps1
  - tests/Portal/Workers/Invoke-MfaAction.Tests.ps1
tests:
  - portal/bff/src/routes/mfa-actions.test.ts
related: []
depends_on:
  - T-0221
  - T-0107
needs_scope_review: false
scope_note: ""
---

# T-0224 — Send push notification and set default MFA method

## Report

EPIC-012 SPEC.md §3.2 (US-4, US-5): **Send push** targets a registered device, and **Default
method** selects from the user's registered methods. §4.2 routes both through EPIC-006 (confirm,
apply, audit).

Add `POST /v1/tenants/{id}/users/{userId}/push` and
`POST /v1/tenants/{id}/users/{userId}/default-method` to `mfa.ts`, and the
`Invoke-MfaAction.ps1` worker job plus `invoke-mfa-action.ps1` entrypoint. Setting a default
method is rejected when the requested method is not in the user's registered set; sending a push
is rejected when no push-capable device is registered. Both actions are audited.

## Acceptance

- [ ] Push is sent only to a registered device and the result is reported.
- [ ] Default method is accepted only from the registered set; an unregistered method returns a structured 4xx.
- [ ] Both actions route through EPIC-006 and write an `AuditEvent`.
- [ ] A test covers the change and fails without it.
- [ ] No file outside `scope` is modified.
