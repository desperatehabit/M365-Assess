---
id: "T-0204"
source: "docs/portal-specs/01-feature-epics/EPIC-011-users-offboarding/SPEC.md"
section: "Users — lifecycle"
severity: "high"
status: "open"
original_status: "declared"
parent: "EPIC-011"
scope:
  - portal/bff/src/routes/users.ts
  - portal/bff/src/routes/users-actions.test.ts
  - portal/workers/M365Portal.Workers/Invoke-UserAction.ps1
  - portal/workers/invoke-user-action.ps1
  - tests/Portal/Workers/Invoke-UserAction.Tests.ps1
tests:
  - portal/bff/src/routes/users-actions.test.ts
related: []
depends_on:
  - T-0202
  - T-0107
needs_scope_review: false
scope_note: ""
---

# T-0204 — User lifecycle actions API

## Report

EPIC-011 SPEC.md §3.1 and §4.3 (US-4): reset password, require password change, revoke sessions,
disable/enable, and restore a deleted user are each a **gated write** routed through EPIC-006,
confirmed and audited. Add
`POST /v1/tenants/{id}/users/{userId}/actions/{action}` to `users.ts` covering the lifecycle
action set, and implement the `Invoke-UserAction.ps1` worker job plus `invoke-user-action.ps1`
entrypoint.

Each action uses the EPIC-006 gated executor (T-0107): destructive or session-breaking actions
(revoke sessions, disable, delete-restore) require confirmation, capture before/after, and write
an `AuditEvent`. Restore is available only for a soft-deleted user and must surface the restored
state. Unknown action names are rejected with a structured error, never passed through.

## Acceptance

- [ ] Each lifecycle action in the §4.3 set is dispatched by the route and executed by the worker.
- [ ] Reset password returns any one-time value without persisting or logging it.
- [ ] Every action is confirmed where destructive and writes before/after plus an `AuditEvent`.
- [ ] An unknown action returns a structured 4xx, not a silent no-op.
- [ ] A test covers the change and fails without it.
- [ ] No file outside `scope` is modified.
